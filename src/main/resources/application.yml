spring:
  profiles:
    active: dev
  application:
    name: demo
  graphql:
    graphiql:
      enabled: true
    schema:
      printer:
        enabled: true
  mongodb:
    uri: ${MONGODB_URI}

server:
  port: 8080

logging:
  level:
    root: INFO

api:
  base-url: api/v1